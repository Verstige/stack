[build]
builder = "docker"
image = "ghcr.io/verstige/stack:latest"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=8080"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[run]
command = "php artisan serve --host=0.0.0.0 --port=8080"
port = 8080